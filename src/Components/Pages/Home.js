import React from "react";
import Layout from "../Core/Layout";
import SearchBar from "../Core/SearchBar";

const Home = () => {
    return(
        <Layout>
           <SearchBar />
        </Layout>
    )
};

export default Home;